#include <iostream>
#include <string>
using namespace std;
int main()
{
    string s1="hello", s2="its line"; //объявление класса
    cin >> s1; //ввод и вывод строки стандартен
    s1.erase(n, 1); //удалить с позиции index (n) количество символов count (1)
    s1.insert(3, " world!"); //вставить в строку s1 начиная с позиции 3 строку " world!"
    s1.find("he", 1); //найди в строке s1 подстрочку "he", поиск начинать с первого символа
    //find возвращает индекс первого вхождения, также можно просто s1.find("he");
    s1.rfind("he"); //поиск с конца, находит ПОСЛЕДНИЙ символ вхождения строки "he"
        //с помощью find и rfind можно найти индекс начала и конца интересующей нас подстроки в строке
    s1.replace(1, 2, s2); //заменить 2 символа, начиная с позиции 1 на символы строки s1
    s1.length(); //длина (кол-во символов) строки s1
    s1.begin(); //начало строки
    s1.end(); //конец строки
    s1.size(); //размер строки в байтах
	getline(cin, s1, ' '); //считать слово из строки до пробела
	getline(cin >> ws, s); //считать текст игнорируя все предыдущие вводы
}